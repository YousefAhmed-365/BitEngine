================================================================================
                       BITENGINE COMPREHENSIVE FEATURE LIST
================================================================================

--------------------------------------------------------------------------------
1. BITSCRIPT NARRATIVE ENGINE (v0.3)
--------------------------------------------------------------------------------
BitEngine now uses a centralized data-driven `project.json` architecture while the logic executes on a strictly bytecode-driven high-performance custom VM.

A. BITSCRIPT LANGUAGE FEATURES
    - Scene Management: `scene scene_id(params) { ... };` blocks with support for nesting and parameters.
    - Global State: `var identifier = value;` (Supports both integer and string values).
    - Local State: `local identifier = value;` for scene-scoped variables (pushed/popped with call stack).
    - String Variables: Seamless support for string type variables (`local name = "John";`).
    - Dynamic Resolution: Universal `{var_name}` syntax to dynamically resolve assets and strings across all commands.
    - Dialogue Syntax: `entity_id.alias [modifiers]: "text content";`
      - Character Aliases: Define named "moods" or shortcuts in entity blocks.
    - Narration Syntax: `narration [modifiers]: "text content";` (Standalone narrator text).
    - Character Management:
      - `> entity_id [modifiers];` - Join character to scene (sets alpha=1.0 by default).
      - `< entity_id;` - Remove character from scene (unloads from active map).
    - Entity Block Expansions: Characters can group actions contextually.
      - `.move <target> <duration>;` and `.fade <alpha> <duration>;` directly inside `{}` blocks.
    - Cinematic Statements: Standalone space-separated commands (no commas) for better readability.
      - `fade_screen <alpha> <duration> [wait];` (Manual full-screen opacity control).
      - `fade <entity_id> <alpha> <duration> [wait];` (Smooth per-entity opacity interpolation).
      - `ui <show/hide/activate/deactivate/set> ...;` (Unified global visibility and layout control).
      - `wait <move/fade/all/sfx/timeline>;` (Explicitly pause VM until visual/audio actions finish).
    - Narrative Stack: Sub-routine support with parameter passing.
      - `call <scene_id>(args);` - Jump to scene, push return address, and inject arguments as locals.
      - `return;` - Pop from stack, clear locals, and return to caller.
    - Comments:
      - `# Single-line comment.`
      - `#- Multi-line comment block -#`
    - Branching: `choice { "Option A" -> target_scene; ... };` with conditional 
      visibility via `if (expression)`.
    - Logic: Robust expression evaluator supporting arithmetic (`+`, `-`, `*`, `/`), 
      logical (`and`, `or`, `!`), and comparison (`==`, `!=`, `<`, `>`, `<=`, `>=`).
      - Support for compound assignment operators (`+=`, `-=`, `*=`, `/=`, `++`, `--`).
      - Full variable interpolation and type-checking within expressions.
      - Time suffixes (`ms`, `s`) directly supported in numerical values.
      - Cinematic statement arguments (duration, alpha, positions) fully accept dynamic expressions.
      - Conditional Ternary Modifiers: `[modifier = (cond ? true_val : false_val)]` directly inside `[]` blocks.
    - Control Flow: `if (condition) { ... }`, `jump scene_id;`, and `delay ms;`.

B. NATIVE VIRTUAL MACHINE (VM)
    - Bytecode Execution: Compiled `.bitc` files execute O(1) instruction dispatch.
    - Program Counter (PC): Precise tracking of execution state across saves.
    - Narrative Stack: Full LIFO stack support for nested `call`/`return` logic.
    - Error Handling: Runtime validation for missing labels, variables, or entities.
    - Input Decoupling: UI interaction is now completely decoupled from narrative input lockouts. 
      Buttons and UI elements remain responsive even when the narrative is "waiting" or revealing text.

--------------------------------------------------------------------------------
2. RICH TEXT RENDERING SYSTEM
--------------------------------------------------------------------------------
Fully procedural text engine supporting nested style tags and dynamic reveal.

A. FORMATTING TAGS
   - [color=#HEX / "NAME"]: Overrides text color (e.g., `[color=RED]Alert![/color]`).
   - [speed=FLOAT]: Modifies typewriter reveal speed (e.g., `[speed=0.5]Slow...[/speed]`).
   - [wait=FLOAT]: Pauses the reveal for X seconds at the current character.
   - [shake]: Adds procedural per-character positional jitter for intensity.
   - [wave]: Adds procedural per-character sine-wave vertical offset for "ethereal" text.
   - [font=ID]: Switches font mid-sentence using IDs registered in the style manager.
   - [/tag]: Closes active modifiers to return to default styling.

B. DYNAMIC FEATURES
   - Variable Interpolation: Automatically replaces `{var_name}` with registry values.
   - Neural Typewriter: Variable reveal speed based on punctuation and character modifiers.
   - Advance Width Caching: High-performance font measurement to maintain stable FPS.

--------------------------------------------------------------------------------
3. VISUAL STYLING & DATA-DRIVEN UI SYSTEM
--------------------------------------------------------------------------------
Comprehensive JSON-driven layout and styling architecture completely decoupled from engine logic.

A. HIERARCHICAL LAYOUT SYSTEM (`ui_default.json`)
   - Adaptive Anchor Framework: Supports 10 anchor points (e.g., `top-center`, `fill`).
   - Resolution Independent Sizing: Supports pixels, `auto` (content wrap), and percentages (`"50%"`).
   - Component Binding: maps engine data to elements based on a `role`.
   - List Templating: `bind_items` allows generating lists from engine arrays using an `item_template`.
   - Reactive Data Binding: Standard JSON pointer syntax (`var.player.health`) for all properties.
   - Lifecycle Management: `ui_activate` and `ui_deactivate` commands with suspension support.
   - Advanced Asset Registration: Define UI path, layer, and initial state in the `assets` block.
   - Identifier Syntax: UI commands now use clean identifiers instead of quoted strings.

B. GLOBAL STYLESHEETS (`style.json`)
   - Reusable Property Blocks: Named style presets that define borders, colors, text sizing, and textures.
   - Inline Overrides: UI components can override inherited styles directly within their element nodes.
   - Explicit Visibility Control: Dedicated `hide: true` flag in style blocks to force-hide elements regardless of content.
   - Standardized Cursor System:
     - `mouse_cursor`: Custom skinning for the system pointer with automatic hardware cursor hiding.
     - `dialog_cursor`: Style-driven "waiting" indicator shown when narrative input is required.

C. TEXTURE OVERRIDES & EFFECTS
   - Support for custom textures for Dialog Boxes, Choice Boxes, Toasts, and Cursors.
   - Nine-Slice Scaling: Define slice margins to scale UI panels without distortion.
   - Color Tints: Multiplicative blending to recolor textures on the fly.

--------------------------------------------------------------------------------
4. CINEMATIC & VFX SYSTEMS
--------------------------------------------------------------------------------
Native support for dramatic narrative presentation and cinematic effects.

A. ENTITY SYSTEM
   - Multi-Sprite Entities: Map character IDs to various expressions/poses.
   - Animations: Support for frame-based sprite sheet animations (`path`, `frames`, `speed`).
   - Procedural Floating: Gentle breathing/floating effects defined in global styles.
   - Dynamic Shadows: Automatic character shadows with adjustable opacity.

B. CINEMATIC COMMANDS
   - `move`: Smooth cubic-eased position interpolation over time.
   - `fade`: Linear opacity interpolation for characters (0.0 to 1.0) and backgrounds.
   - `fade_screen`: Full-screen cinematic color overlays with precise linear alpha interpolation.
   - `shake`: Procedural camera/UI jitter with intensity control.

C. DIRECT CINEMATIC CONTROL
   - Pure explicit cinematic flow powered by manual `fade_screen` replacing all automated/rigid transition mechanics.
   - Visual Continuity: Background/Character fades continue independently across script execution.

--------------------------------------------------------------------------------
5. AUDIO & ASSET MANAGEMENT
--------------------------------------------------------------------------------
- Background Music (BGM): `bgm <track_id> [loop=true];` (Continuous looping tracks with automatic caching).
- Sound Effects (SFX): `sfx <track_id>;` (One-shot triggers with memory management and silent fallbacks).
- Asset Auto-Discovery: Dynamically discovers images, sounds, and fonts from the `assets/` subdirectories, eliminating hardcoded asset manifest blocks.
- Strict Asset Validation: Native `strict_assets` engine flag to halt the game with clear console logs when referenced assets are missing, or gracefully fallback to magenta placeholders when disabled.
- Global Registry: Centralized mapping for backgrounds, music, and sfx IDs.
- XOR Security: Native encryption for project files and compiled bytecode.

--------------------------------------------------------------------------------
6. DEVELOPER TOOLING & CLI
--------------------------------------------------------------------------------
The engine provides a robust toolset for building and debugging narratives.

A. COMMAND LINE INTERFACE (BitEngine CLI)
   - `-c, --compile <project.json>`: Compiles all scripts in the project into encrypted bytecode.
   - `-r, --run <project.json>`: Runs the specified project directly from source or compiled binary.
   - `-d, --debug`: Starts the engine with the unified debug overlay active.
   - `-s, --style <id>`: Launches with a specific UI style preset.

B. HOT-RELOADING
    - Instant live-updates to UI layouts (`ui/`) and entity properties (`entities/`) when their JSON source files are modified during gameplay, enabling seamless rapid iteration.

C. UNIFIED DEBUG OVERLAY (F3)
    - Bytecode Inspector: Real-time visualization of the running VM bytecode with 
      PC tracking, argument sanitization, and metadata analysis.
    - Local Scope & Stack: Specialized panel for monitoring call depth, return 
      addresses, and active `local` variables in the current scene.
    - Variable Watcher: Real-time list of all global variables and their values.
    - Event Trace: Chronological telemetry log of variable changes (Old -> New), 
      branching decisions (IF TRUE/FALSE), and Scene Jumps.
    - Asset & Memory Audit: Live tracking of cached textures, fonts, sounds, and music, including the path of the currently active BGM.
    - UI Layout Manager State: Real-time visualization of all loaded UI layers, their Z-order, visibility flags, and active state.
    - Static Analyzer: Proactive narrative validation during compilation.
      - Referential Integrity: Detects broken `jump`, `call`, and `choice` targets.
      - Variable Audit: Flags use of undefined variables in expressions and conditionals.
      - Asset Validation: Ensures character IDs and event targets exist in registry.
      - Line-Linked Errors: Directly maps bytecode issues back to source line numbers.

--------------------------------------------------------------------------------
7. INTERACTION & ACCESSIBILITY
--------------------------------------------------------------------------------
- Message History (H): Complete scrollable backlog of all past dialogue and effects.
- Auto-Play Mode (A): Hands-free progression that respects reveal speeds.
- Fast Skip: Advance current text reveal instantly on click/space.
- Neural Saves: Binary save system that preserves variable state and exact PC.
- Toast System: Non-blocking notifications for game events (e.g., "Game Saved").
- Custom Cursor: Style-driven mouse textures with automatic system cursor hiding.